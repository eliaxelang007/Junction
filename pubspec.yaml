name: junction
description: Junction is a pure Dart library (no Flutter SDK required) that provides an API for working with the native filesystem, the web filesystem, and web origin private file filesystem (OPFS)!
version: 1.1.3
repository: https://github.com/eliaxelang007/Junction

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  path: ^1.9.1
  web: ^1.1.1

  # file_selector: ^1.1.0
  # cross_file: ^0.3.5+2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
