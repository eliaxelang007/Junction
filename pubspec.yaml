name: junction
description: Junction is a library that provides an API for working with the filesystem in both web and native environments!
version: 1.1.1
repository: https://github.com/eliaxelang007/Junction

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  path: ^1.9.1
  web: ^1.1.1

  # file_selector: ^1.1.0
  # cross_file: ^0.3.5+2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
